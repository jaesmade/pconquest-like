# Scalability reassessment: 32×32, competitive 8v8

Assessment date: 2026-09-26. Target scope is a square map up to 32×32 tiles, two human players with up to eight deployed Pokémon each, up to 20 total owned Pokémon per player in a run, and at least 60 rendered frames per second. The current build is an 8×8, local, single-player prototype with six owned Pokémon and three deployed. This document records the work needed to meet the new limits; it does not claim that the current build meets them.

## Evidence and frame budget

At 60 FPS, each frame has 16.67 ms for browser scripting, rendering, and compositing. `node scripts/measure-save.mjs` measures only Node-side state operations on synthetic maps. The 32×32, 16-unit, 20-owned scenario produces a roughly 12 KB compact save, with command cloning and JSON serialization well below 1 ms on the local machine. Those costs are small relative to the frame budget. The benchmark does not measure IndexedDB latency or network traffic. A separate [32×32 browser rendering profile](RENDER_PROFILE.md) now measures the minified Phaser board at 8v8 and records frame intervals, draw calls, JavaScript heap, and long tasks. Its synthetic scene stayed near 60 Hz on the reference browser; GPU time and slower devices remain unmeasured.

A far pursuit on an open 32×32 map required 14 batches of 32 search nodes, with 2.65 ms total search CPU. At one batch per rendered frame, that spans about 0.23 seconds at 60 FPS before the unit acts. This is a solo/bot responsiveness cost to watch; competitive human matches do not run this planner. The measurements vary with hardware and map layout.

The current `Board` creates a Phaser canvas at `64 × map width` by `64 × map height`. At 32×32, that is 2048×2048 pixels, or 16 MiB for a single RGBA surface before renderer buffers and textures. It draws all 1,024 terrain cells and creates separate text objects for terrain details. The browser profile found target cursor movement unnecessarily rescanned the whole map, so target preview drawing now updates separately. Full scans remain for battle and mode changes. Fit-to-page scaling can still make targeting difficult on narrow screens. The measured profile does not justify a chunked rendering rewrite.

## Priorities

| Priority | Area | Current gap | Required change | Acceptance evidence |
| --- | --- | --- | --- | --- |
| P0 | Performance gate | The synthetic 32×32, 16-unit browser profile exists and the measured targeting redraw hotspot is optimized. GPU time, texture memory, slower hardware, and a full competitive match remain unmeasured. | Profile representative full-match play on the release reference device, including camera navigation and maximum effects. Record GPU/frame traces and texture memory before changing terrain rendering. | At least 60 FPS on a declared reference browser/device under the worst expected 32×32 combat scene, with CPU/GPU frame traces and memory recorded. |
| P0 | Online battle authority | React commits local commands directly; `Run` and `Battle` have one local player and `player`/`enemy` sides but no owning participant. Docker serves static files only. | Extract a pure command reducer shared by browser and a server. Server validates actor ownership, turn, AP, move, position, inventory, and target; owns RNG, unit IDs, revisions, and outcomes. Treat browser state as a view/prediction layer. Add WebSocket match transport, reconnect snapshot, and idempotent command IDs. | Two clients play a full 8v8 match, reject stale/duplicate/invalid commands, reconnect to the same revision, and agree on a replayed state hash. |
| P0 | Roster and deployment | Save validation caps `party` at six; UI, recruitment, and engine cap deployment at three. Maps now define ally, neutral, and enemy zones with room for eight per side, but solo preparation still selects at most three. | Model up to 20 owned Pokémon and an ordered lineup of up to eight, with the other 12 in reserve. Give each PvP participant their own roster and lineup. Preserve v9 zone choices during the roster migration. | A 20-owned roster survives save/load and can deploy eight; a match loads 8v8 with unique valid positions and correct rewards. |
| P0 | Match rules and determinism | Engine creates UUIDs during battle setup and visual events; local AI auto-acts for the opposing side. | Record stable authoritative entity IDs in the initial match state and route every rule random draw through the saved match RNG. Separate cosmetic event IDs from rule state. Make both sides human-commandable, add match outcome/timeout/disconnect rules, and version rules and map content for replay. | Replaying the same initial snapshot and command log produces identical authoritative hashes on server and client builds. |
| P1 | Board navigation and rendering | A 2048×2048 full-map canvas can be memory-heavy, and fit-to-page scaling can make 32×32 tile selection difficult. Pointer coordinates currently assume camera origin zero. | Add camera pan/zoom and correct screen-to-world pointer conversion if the target screen needs them. Cache overlays and cull labels/terrain chunks only if profiling shows a frame or memory problem; avoid a rendering rewrite solely from tile count. | Tiles remain easy to select at the target screen size; measured frame time and memory stay within the release budget. |
| P1 | Pathfinding and targeting | A far open-grid pursuit used 14 planner batches; move/attack previews still redraw across every cell. | Keep bounded planning. Use heuristic search or overlay caching if representative maps exceed the interaction latency or frame budget. Invalidate caches on occupancy, terrain, target, or rule changes. | Far pursuit and overlays respond within a defined interaction latency budget without long main-thread tasks. |
| P1 | Animation pacing | The renderer now holds at most three waiting attack cues and one playing cue. It coalesces stale weather and repeated empty-target cues and shortens playback when behind; the solo UI still waits for retained cues. | Profile an 8v8 round with area effects, cull offscreen effects, and set a measured playback latency budget for future networked turns. | An 8v8 round with area effects does not accumulate an input-blocking animation backlog. |
| P1 | Save and account ownership | IndexedDB v12 is suitable for local runs, but the browser can edit it. | Store competitive match state and account roster on the server. Use local IndexedDB only for cache/recovery of offline runs. Define the next schema migration, server persistence, content version checks, and conflict handling across devices. | Edited local saves cannot alter ranked match state; reconnect and cross-device login restore the server roster and match. |
| P2 | Capacity and content | No server, concurrency limit, asset budget, or capped-map content pipeline exists. | After one-match correctness, set concurrent-match and latency targets, profile server CPU/memory per match, and load-test. Validate map dimensions ≤32, 8+8 spawns, tile effects, and asset memory at content import. | Document measured matches per server instance, network bytes per command, and client asset/memory budgets. |

## Architecture decisions for the next build

1. Keep the tactical rules independent of Phaser, React, storage, and sockets. A command should be plain data such as `{ matchId, commandId, expectedRevision, actorUnitId, action }`; the reducer returns the next authoritative state and ordered rule events. The server is the only writer in competitive matches.
2. Send commands and compact state deltas rather than an entire map after every action. Send the authored map ID/content version once, a complete snapshot on join/reconnect, then revisioned unit and tile changes. Reject or resync when a client misses a revision.
3. Keep 60 FPS as a rendering target separate from network command latency. Turn resolution can wait for the server without blocking pan, hover, menus, or cosmetic playback. Set a reference browser, device, resolution, and device-pixel ratio before claiming compliance.
4. Preserve local solo mode as a separate match adapter that calls the same reducer. A bot submits commands through the same validation boundary; it cannot mutate authoritative state directly.

## Release gate for these limits

Profile a production build on a declared reference device with a 32×32 map, 16 battle units, 20 owned Pokémon per participant, hazards, weather, area attacks, board navigation, and the maximum expected animation load. Record p50/p95/p99 frame time, main-thread long tasks, GPU time, canvas/texture memory, command latency, snapshot/delta bytes, reconnect time, and server CPU/memory per match. The current Node benchmark only clears the command-clone and save-size concern; it does not establish the 60 FPS target or multiplayer capacity.
